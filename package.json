{
  "name": "latest-solid",
  "type": "module",
  "scripts": {
    "dev": "vinxi dev",
    "build": "vinxi build",
    "build.cloudflare": "vinxi build --preset cloudflare-pages",
    "wrangler": "pnpm run build.cloudflare && npx wrangler pages dev dist --compatibility-flag=nodejs_compat",
    "start": "vinxi start"
  },
  "dependencies": {
    "@solidjs/meta": "^0.29.2",
    "@solidjs/router": "^0.10.10",
    "@solidjs/start": "^0.4.10",
    "solid-js": "^1.8.12",
    "vinxi": "^0.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}